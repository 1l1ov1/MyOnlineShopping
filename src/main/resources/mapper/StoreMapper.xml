<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wan.mapper.StoreMapper">

    <insert id="insertStore">
        insert into store(user_id, store_name, status, create_time, update_time)
        VALUES (#{userId}, #{storeName}, #{status}, #{createTime}, #{updateTime})
    </insert>
    <update id="update">
        update store
        <set>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="storeName != null">
                store_name = #{storeName},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteByIds">
        delete from store where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>
    <select id="pageQuery" resultType="com.wan.vo.StorePageQueryVO">
        SELECT
        s.id,
        s.user_id,
        s.store_name,
        s.`status` ,
        u.username,
        s.create_time
        FROM
        store s
        INNER JOIN `user` u ON `u`.id = s.user_id
        <where>
            <if test="username != null and username != ''">
                and username like concat('%', #{username}, '%')
            </if>
            <if test="storeName != null">
                and store_name like concat('%', #{storeName}, '%')
            </if>
            <if test="status != null">
                and s.status = #{status}
            </if>
        </where>
        order by s.create_time
    </select>
    <!--查询商店的详情-->
    <select id="getStoreDetail" resultType="com.wan.vo.StorePageQueryVO">
        SELECT s.id,
               s.user_id,
               s.store_name,
               s.`status`,
               u.username,
               s.create_time
        FROM store s
                 INNER JOIN `user` u ON `u`.id = s.user_id
        where s.id = #{id}
    </select>
</mapper>